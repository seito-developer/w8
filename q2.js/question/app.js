(()=>{
  const $button = ; //要修正
  const $modal = ; //要修正

  const clickHandler = () => {
    $modal.classList.toggle('is-active');
  };

  $button.addEventListener('click', () => clickHandler());
})();